name: AI Technology Daily News

on:
  schedule:
    # 每天北京时间 9:30 执行 (UTC 1:30)
    - cron: '30 1 * * *'
  workflow_dispatch:

jobs:
  generate-report:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4

      - name: Generate AI Daily Report
        env:
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
        run: python scripts/generate_ai_report.py

      - name: Commit report
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Action"
          git add reports/
          git diff --quiet && git diff --staged --quiet || git commit -m "📰 AI技术资讯日报 - $(date '+%Y-%m-%d')"
          git push
